name: Publish Images

on:
  push:
    branches: [main]

jobs:
  publish:
    runs-on: gha-homelab
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        run: docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        run: |
          docker build -f Dockerfile.backend -t ghcr.io/michaelmuz/expense-splitter-backend:${{ github.sha }} -t ghcr.io/michaelmuz/expense-splitter-backend:latest .
          docker push ghcr.io/michaelmuz/expense-splitter-backend:${{ github.sha }}
          docker push ghcr.io/michaelmuz/expense-splitter-backend:latest

      - name: Build and push frontend
        run: |
          docker build -f Dockerfile.frontend -t ghcr.io/michaelmuz/expense-splitter-frontend:${{ github.sha }} -t ghcr.io/michaelmuz/expense-splitter-frontend:latest .
          docker push ghcr.io/michaelmuz/expense-splitter-frontend:${{ github.sha }}
          docker push ghcr.io/michaelmuz/expense-splitter-frontend:latest
